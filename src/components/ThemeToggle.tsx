"use client";

export default function ThemeToggle() {
  return (
    <button
      className="hidden"
      aria-label="Toggle theme"
    >
      {/* Dark mode only - toggle hidden */}
    </button>
  );
}